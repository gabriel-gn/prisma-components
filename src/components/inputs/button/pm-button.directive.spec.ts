import { PmButtonDirective } from './pm-button.directive';

describe('PmButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PmButtonDirective();
    expect(directive).toBeTruthy();
  });
});
