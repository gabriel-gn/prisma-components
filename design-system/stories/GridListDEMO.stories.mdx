import {Meta, Story, ArgsTable, Canvas} from '@storybook/addon-docs';
import {ButtonComponent} from '../projects/components/src/lib/button/button.component';
import {MainColors} from "../projects/components/src/models/colors";
import {moduleMetadata} from "@storybook/angular";
import {ButtonModule} from "../projects/components/src/lib/button/button.module";
import {DefaultButton, PrimaryButton} from "./inputs/Button.stories";

<Meta
  title='Prisma/Inputs/Buttons'
  component={ButtonComponent}
  decorators={[
    moduleMetadata({
      declarations: [ButtonModule]
    })
  ]}
/>

export const defaultArgs = {
  type: MainColors.default,
  label: 'Button',
};

export const defaultControls = {
  label: {control: 'text'},
  type: {control: 'select', options: Object.values(MainColors),},
  busy: {control: 'boolean'},
  busyText: {control: 'text'},
  iconClass: {control: 'text'},
  outline: {control: 'boolean'},
};

export const Template = (args) => <ButtonComponent {...args} />

Some **markdown** description, or whatever you want.

<Canvas>
  <Story name='Default' story={DefaultButton}></Story>
  <Story name='Primary' story={PrimaryButton}></Story>
  <Story id='prisma-inputs-buttons--default-button'></Story>
</Canvas>

## Args Table

<ArgsTable of={ButtonComponent}/>
